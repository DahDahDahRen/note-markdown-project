import styled from "styled-components";

export const NavBar = styled.nav`
  padding: 0 24px;

  .nav-logo {
    font-size: 14px;
    font-weight: 600;
    line-height: 24px;
    padding: 40px 0;

    display: flex;
    gap: 12px;
  }

  .nav-add-notebook {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .nav-add-notebook p {
    font-size: 14px;
    font-weight: 600px;
    line-height: ;
  }
`;
