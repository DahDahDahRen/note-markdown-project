import styled from "styled-components";

export const StyledNotebookPageItem = styled.li`
  display: flex;
  justify-content: space-between;

  padding: 8px 0;

  .page-detail {
    display: flex;
    gap: 8px;
  }

  .page-title {
    font-size: 12px;
  }
`;
